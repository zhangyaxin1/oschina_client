<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.day1_1_hellowrold"
    android:versionCode="1"
    android:versionName="1.0" >
    <!-- 指定支持的android版本14-4.0;23-6.0 -->
    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="23" />
    <!-- 整个app ;icon:安装后显示的图标;label:安装后显示的名称;theme:皮肤-->
    <application
        android:allowBackup="true"
        android:icon="@drawable/a"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- 添加一个窗口;name:activity的类名;label:安装后显示的名称 -->
        <activity
            android:name="com.example.day1_1_hellowrold.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       
    </application>

</manifest>
